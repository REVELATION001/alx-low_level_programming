#include <stdio.h>

/**
 * main - Entry point
 * void - empty
 * Return: Always 0 (Success)
 */
int main(void)
{
	char o;

	for  (c = 'a'; o <= 'z'; o++)
	{
		putchar(o);
	}
	putchar('\n');
	return (0);
}
